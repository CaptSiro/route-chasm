<?php /** @var CallStack $this */

use components\core\CallStack\CallStack;

?><div class="trace">
    <?php foreach ($this->stack as $entry): ?>
        <div>
            <code><?= $this->getEntryClass($entry) ?><?= $entry['function'] ?>(...) in <?= $entry['file'] ?>:<?= $entry['line'] ?></code>
        </div>
    <?php endforeach; ?>
</div>
