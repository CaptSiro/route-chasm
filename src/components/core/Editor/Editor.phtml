<?php /** @var Editor $this */

use components\core\Editor\Editor;
use components\core\Html\Html;
use core\forms\Form;
use core\sideloader\importers\Css\Css;
use core\sideloader\importers\Javascript\Javascript;

Css::import($this->getResource('editor.css'));
Css::import(Form::getStaticResource('form.css'));
Javascript::import($this->getResource('editor.js'));
Javascript::import($this->getResource('inspector.js'));
Javascript::import($this->getResource('widget-core.js'));
Javascript::import(Form::getStaticResource('form.js'));

?><section>
    <div id="editor-data" style="display: none">
        <?= Html::escape($this->storage->read()) ?>
    </div>

    <div id="widgets" style="display: none">
        <?php foreach ($this->explodeWidgets() as $widget): ?>
            <?= $this->importWidget($widget) ?>
        <?php endforeach; ?>
    </div>
</section>

<section class="editor">
    <?= $this->toolBar ?>

    <div class="table">
        <div class="viewport-mount">
            <div id="viewport">
                <div id="widget-select-mount" style="visibility: hidden"></div>
            </div>
        </div>

        <div class="inspector">INSPECTOR</div>
    </div>
</section>