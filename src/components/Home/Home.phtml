<?php /** @var Home $this */

use components\core\PageMenu\Footer;
use components\core\PageMenu\Header;
use components\Home\Home;
use core\sideloader\importers\Css\Css;

Css::import($this->getResource('home.css'));

$docsLink = $this->createDocsLink();
$projectLink = $this->getProjectLink();
$projectName = $this->getProjectName();

?><?= Header::default() ?>

<main class="home-landing page-container">
    <div class="home-container">
        <div class="home-hero">
            <h1><?= $this->tr('Build full-stack apps without fighting your framework') ?></h1>

            <p class="home-subtitle">
                <?= $this->tr('RouteChasm is a developer-first PHP framework focused on clarity, flexibility, and control - not magic, not bloat.') ?>
            </p>

            <div class="home-actions">
                <?php if (!is_null($projectLink)): ?>
                    <a href="<?= $projectLink ?>" class="button">
                        <?= $this->tr('Start Building with') ?>
                        <?= $projectName ?>
                    </a>
                <?php endif; ?>

                <?php if (!is_null($docsLink)): ?>
                    <a href="<?= $docsLink ?>" class="button">
                        <?= $this->tr('View Documentation') ?>
                    </a>
                <?php endif; ?>
            </div>

            <div class="home-bullets">
                <span><?= $this->tr('Minimal setup') ?></span>
                <span><?= $this->tr('Full control') ?></span>
                <span><?= $this->tr('No hidden behavior') ?></span>
            </div>
        </div>

        <div class="home-section">
            <h2><?= $this->tr('Designed for control, not convention lock-in') ?></h2>
            <p>
                <?= $this->tr('Most frameworks push rigid patterns early. RouteChasm doesn’t. You start simple - and stay in control as complexity grows.') ?>
            </p>
        </div>

        <div class="home-grid">
            <div class="home-card">
                <h3><?= $this->tr('Modular by default') ?></h3>
                <p><?= $this->tr('Every part of your application is composable and replaceable.') ?></p>
                <ul>
                    <li><?= $this->tr('Swap logic without rewrites') ?></li>
                    <li><?= $this->tr('Build only what you need') ?></li>
                    <li><?= $this->tr('Explicit dependencies') ?></li>
                </ul>
            </div>

            <div class="home-card">
                <h3><?= $this->tr('Database interaction that makes sense') ?></h3>
                <p><?= $this->tr('Work with your data without ORM friction.') ?></p>
                <ul>
                    <li><?= $this->tr('Simple CRUD out of the box') ?></li>
                    <li><?= $this->tr('Easy overrides') ?></li>
                    <li><?= $this->tr('Clear mapping to database') ?></li>
                </ul>
            </div>

            <div class="home-card">
                <h3><?= $this->tr('Flexible form handling') ?></h3>
                <p><?= $this->tr('Forms adapt to your needs, not the other way around.') ?></p>
                <ul>
                    <li><?= $this->tr('Custom labeling & mapping') ?></li>
                    <li><?= $this->tr('Unified configuration') ?></li>
                    <li><?= $this->tr('No scattered overrides') ?></li>
                </ul>
            </div>

            <div class="home-card">
                <h3><?= $this->tr('Predictable architecture') ?></h3>
                <p><?= $this->tr('You always know where things are and how they behave.') ?></p>
                <ul>
                    <li><?= $this->tr('Consistent structure') ?></li>
                    <li><?= $this->tr('Minimal magic') ?></li>
                    <li><?= $this->tr('Easier debugging') ?></li>
                </ul>
            </div>
        </div>

        <div class="home-section">
            <h2><?= $this->tr('Simplicity over automation') ?></h2>
            <p>
                <?= $this->tr('Automation often hides complexity instead of removing it. RouteChasm keeps things explicit so you can reason about your system.') ?>
            </p>
        </div>

        <div class="home-cta">
            <h2><?= $this->tr('Stop working around your framework') ?></h2>
            <p><?= $this->tr('Build something that stays understandable - even after it grows.') ?></p>

            <?php if (!is_null($projectLink)): ?>
                <a href="<?= $projectLink ?>" class="button">
                    <?= $this->tr('Start Building with') ?>
                    <?= $projectName ?>
                </a>
            <?php endif; ?>
        </div>
    </div>
</main>

<?= Footer::default() ?>
