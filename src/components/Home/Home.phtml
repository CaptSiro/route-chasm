<?php /** @var Home $this */

use components\core\fs\FileControl;
use components\Home\Home;
use core\fs\variants\ImageVariant;
use modules\locales\EnglishUS;

?><h1>Route Chasm</h1>
<a href="https://github.com/CaptSiro/route-chasm">GitHub repository</a>

<p>Navigate to <code>src/components/Home/Home.phtml</code> to edit this page</p>

<hr>
<h3>Languages</h3>
<span><?= $this->trg('home', 'Selected') ?>: <?= \core\App::getInstance()->getRequest()->getLanguage()->getLocale()->getName() ?></span>
<?php foreach (\core\App::getInstance()->getLocales() as $locale): ?>
    <div>
        <?= $this->createLanguageLink($locale) ?>
    </div>
<?php endforeach; ?>

<div>

    <?= $this->trtg('home', '{} flowers', 0,
        templates: EnglishUS::pluralize('{} flower', '{} flowers')) ?>

</div>
<div><?= $this->trtg('home', '{} flowers', 1, templates: EnglishUS::pluralize('{} flower', '{} flowers')) ?></div>
<div><?= $this->trtg('home', '{} flowers', 2, templates: EnglishUS::pluralize('{} flower', '{} flowers')) ?></div>
<div><?= $this->trtg('home', '{} flowers', 3, templates: EnglishUS::pluralize('{} flower', '{} flowers')) ?></div>
<div><?= $this->trtg('home', '{} flowers', 4, templates: EnglishUS::pluralize('{} flower', '{} flowers')) ?></div>
<div><?= $this->trtg('home', '{} flowers', 5, templates: EnglishUS::pluralize('{} flower', '{} flowers')) ?></div>



<?php $file = \models\core\fs\File::fromId(5) ?>
<img src="<?= $file->getUrl(ImageVariant::get(ImageVariant::TRANSFORMER_ARTICLE_THUMBNAIL)) ?>"
     alt="hom" width="400" height="300">
<img src="<?= $file->getUrl(ImageVariant::get('article-icon')) ?>"
     alt="hom" width="100" height="100">
<img src="<?= $file->getUrlToModel() ?>" alt="hom" width="384" height="216">


<?= new FileControl('image', 'Homepage Image') ?>
